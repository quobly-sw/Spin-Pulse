pages:
  stage: deploy
  dependencies: ["build-doc"]
  script:
    - echo "Publishing docs to Pages"
  artifacts:
    paths:
      - docs_output
  publish: docs_output
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
  environment: production
